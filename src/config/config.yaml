artifact_root: artifacts

data_ingestion:
  root_dir: artifacts/data_ingestion
  source_url: # can be a link to database or web url
  local_data_file: "local_data/winequality-red.csv" # the extraction from sourceurl will be saved here
  unzip_dir: # the downloaded data in zipped format will be unzipped here

data_validation:
  root_dir: artifacts/data_validation
  schema_file_path: src/config/schema.yaml # this file contains the schema of the data
  validation_status_file_path: artifacts/data_validation/status.txt
  
data_transformation:
root_dir: artifacts/data_transformation
transformed_train_data_path: artifacts/data_transformation/train.csv
transformed_test_data_path: artifacts/data_transformation_test.csv


